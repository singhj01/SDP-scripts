#!/usr/bin/env bash

sudo apt-get update
sudo apt install curl git
sudo apt-get install mysql-server openssl
curl -sL https://deb.nodesource.com/setup_8.x | sudo bash -
sudo apt install nodejs
apt-get install npm
